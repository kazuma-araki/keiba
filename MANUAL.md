# 競馬分析ツール マニュアル

このリポジトリは、JRAの成績データを蓄積して基準タイムを作る**データ収集パイプライン（`jra-batch/`）**と、それを使って出走馬を分析する**Webアプリ（`src/`）**の2つから構成されています。

```
jra-batch/ ─┬─ importJraResults.ts   … JRAの成績PDFを取得し、生データを蓄積
            └─ computeBaselines.ts   … 生データから基準タイムを集計

src/ ────────── Webアプリ（ブラウザで動作、静的データをバンドルするだけ・通信なし）
```

すべてブラウザ内 or ローカルスクリプトで完結しており、**実行時の従量課金・通信コストは発生しません。**

---

## 1. データ収集パイプライン（jra-batch）

### 1-1. 何をしているか

JRAが公開している「年度別全成績」PDF（`https://www.jra.go.jp/datafile/seiseki/report/...`）を総当たりで取得し、レース×出走馬単位の生データをローカルに蓄積します。JRAは毎週開催があるため、**同じコマンドを定期的に再実行する運用**を想定しています。

### 1-2. セットアップと実行

```bash
cd jra-batch
npm install

# 対象年のデータを取得（省略時は現在年）
npx ts-node importJraResults.ts 2025

# 動作確認モード（1競馬場・数日分だけに絞る）
npx ts-node importJraResults.ts 2025 --test
```

- 全10競馬場 × 開催回1〜6 × 開催日目1〜12 を総当たりし、実際に存在する開催日だけデータが取れる（それ以外は404として静かにスキップ）
- JRAサーバーへの配慮として、リクエストごとに1秒間隔を空けている

### 1-3. 再実行しても重複しない仕組み（manifest）

`manifest_<年>.json` に「開催回-競馬場コード-開催日目」の取り込み済みキーを記録しており、**同じコマンドを何度再実行しても新規開催分だけが追記されます**（すでに取り込み済みの開催日はリクエストすらしない）。

- ただし404（該当PDFなし）はmanifestに載せません。現在シーズンの未来の開催回・日目は「まだ開催されていないだけ」の可能性があるため、毎回律儀に再チェックします
- 過去年（開催が終わっている年）は多少無駄になりますが、正しさを優先しています

> **注意（スキーマを変更した場合）**：`race_facts_<年>.jsonl` の中身の構造（フィールド）を変更したときは、manifestが「取り込み済み」のままだと古いスキーマのデータが更新されずに残ります。フィールド追加などをした場合は、対象年の `manifest_<年>.json` と `race_facts_<年>.jsonl` を削除してから再実行してください。

### 1-4. 出力ファイル

- `race_facts_<年>.jsonl`：レース×出走馬 1行1レコードのJSON Lines（生データ）
- `manifest_<年>.json`：取り込み済み開催日の一覧

`race_facts_<年>.jsonl` の1レコード例：
```json
{"year":2025,"kaisai":1,"day":1,"raceNumber":1,"location":"札幌","raceDate":"2025年7月26日","distance":1200,"trackType":"芝","condition":"良","raceClassText":"2歳未勝利","grade":null,"raceLast3F":34.7,"quinellaPayout":640,"horseName":"ショウナンカリス","finishRank":1,"timeStr":"1:08.9","totalSeconds":68.9,"odds":4.8,"jockeyName":"北村友一","weight":55,"bodyWeight":408,"bodyWeightChange":4}
```

| フィールド | 内容 |
|---|---|
| `year`/`kaisai`/`day`/`raceNumber` | 開催年・開催回・開催日目・第N競走 |
| `location`/`distance`/`trackType`/`condition` | 競馬場・距離・芝orダートor障害・馬場状態 |
| `raceClassText` | レース条件・レース名の生テキスト（軽くクリーニング済み） |
| `grade` | 重賞グレード（`G1`/`G2`/`G3`/`null`） |
| `raceLast3F` | レース全体の参考上がり3F（秒）。**レース単位の値で、同じレースの馬全員で同じ値になる**（個々の馬の上がり3Fではない） |
| `quinellaPayout` | 馬連配当（円、100円あたり）。**レース単位の値**（同じレースの馬全員で同じ値になる） |
| `horseName`/`finishRank`/`timeStr`/`totalSeconds` | 出走馬名・着順・タイム |
| `odds` | 単勝オッズ。同一レース内の相対比較・回収率検証専用（過去レースの値を別レースの分析に転用しない） |
| `jockeyName` | 騎乗騎手名（`jockeyRoster_<年>.json`の名簿と突き合わせて抽出。空白除去済みの氏名） |
| `weight` | 斤量（kg） |
| `bodyWeight`/`bodyWeightChange` | 馬体重（kg）・前走からの増減（kg、初出走等で不明な場合は`null`） |

### 1-5. 基準タイムの集計

```bash
npx ts-node computeBaselines.ts                          # 全年度のrace_facts_*.jsonlを対象
npx ts-node computeBaselines.ts 2025 2026                 # 対象年を指定
npx ts-node computeBaselines.ts 2025 2026 --groupBy=location,trackType,distance,condition,year  # 切り口を変える
```

デフォルトの集計キーは `location, trackType, distance, condition, grade` です（重賞は同条件の一般戦よりタイムが大きく異なるため）。指定できるフィールドは `year, location, trackType, distance, condition, grade`。

実行すると2つのファイルが生成されます：

- `baseline_<groupBy>_<年>.json`：コース×距離×馬場状態×グレードごとの**秒/mの平均・分散**（全体タイムの基準）
- `baseline_last3f_<groupBy>_<年>.json`：同じキーでの**参考上がり3F（秒）の平均・分散**（レース単位で重複排除して集計）

### 1-5b. 騎手成績の集計とブレンド用データの書き出し

騎手勝率が「タイム指数だけでは説明できない予想外の好走/凡走」と相関することを4期間のウォークフォワード検証（`jockeyDiagnostic.ts`/`jockeyBlendBacktest.ts`）で確認済みです。以下の2ステップで、Webアプリ用の軽量な騎手係数JSONを作ります。

```bash
npx ts-node computeJockeyStats.ts 2025        # 騎手ごとの騎乗数・勝率・複勝率を集計
npx ts-node exportJockeyBlend.ts 2025         # 騎乗数30以上の騎手だけを勝率でzスコア化した軽量JSONを書き出す
```

- `computeJockeyStats.ts` の出力（`jockeyStats_<年>.json`）は騎乗数の少ない騎手も含む生データで、検証・診断用
- `exportJockeyBlend.ts` の出力（`jockeyZScore_<年>.json`）が実際にWebアプリへコピーするファイル

騎手名の抽出には `jockeyRoster_2025.json`/`jockeyRoster_2026.json`（netkeibaの騎手リーディングページから取得した名簿。氏名の突き合わせにのみ使用）が必要です。

### 1-6. Webアプリへの反映

生成されたファイルを、Webアプリ側の静的データとして手動でコピーします（自動化はしていません）。

```bash
cp jra-batch/baseline_location-trackType-distance-condition-grade_<年>.json src/data/baseline.json
cp jra-batch/baseline_last3f_location-trackType-distance-condition-grade_<年>.json src/data/last3fBaseline.json
cp jra-batch/jockeyZScore_<年>.json src/data/jockeyZScore.json
```

---

## 2. Webアプリ（src/）

### 2-1. 起動

```bash
npm install
npm run dev
```

### 2-2. 使い方

1. 画面上部で「今日のレース条件」（芝/ダート/障害・距離）を設定する
2. JRAの出馬表（過去4走モード）のHTMLファイルをアップロード、または過去走テキストをCSV形式で貼り付けて「手動解析」
3. 出走馬ごとの分析結果が表示される

### 2-3. 表示される指標の意味

#### 偏差値（メインの指標）

**今回の出走メンバー内での相対評価**です。中身は生タイムではなく、後述の「基準比較指数」に騎手係数を加味した値を平均50・標準偏差10のスケールに変換したもの。

```
ブレンドスコア = その馬の基準比較指数の加重平均 + 0.1 × 今日の騎乗騎手のzスコア
偏差値 = 50 + (ブレンドスコア − メンバー全体の平均) × 10 ÷ メンバー全体の標準偏差
```

高いほど、今回のメンバーの中で（コース・距離・馬場状態・グレードを補正した上で）強いと期待できる、という意味です。騎手係数（`src/utils/raceWeighting.ts`の`computeJockeyZ`）は、jra-batchのtrainデータから集計した騎手勝率を平均・標準偏差でzスコア化したもので、騎乗数30未満・未収録の騎手は0（平均的）として扱われます。JRA出馬表HTMLのアップロード経由でのみ今日の騎乗騎手が取得でき、CSV手動貼り付け経路では常に0扱いになります（`avgBaselineSpeedIndex`自体は騎手係数を含まない、タイム指数単体の値のまま表示されます）。

#### 基準比較指数（偏差値の中身）

過去走の「秒/m」を、同条件（競馬場・トラック種別・距離・馬場状態・グレード）の歴史的な平均・標準偏差と比べたzスコアです。

```
基準比較指数 = (基準タイムの平均 − その馬の秒/m) ÷ 基準タイムの標準偏差
```

- 0：その条件の平均的なタイム
- プラスが大きいほど基準より速い（好走）。目安として+1で上位16%程度、+2で上位2%程度（正規分布を仮定）
- マイナスは基準より遅い

該当する基準タイムが見つからない場合（未知の競馬場名など）は `null` になり、その走は集計に加味されません。

#### 過去走の重み付け（新しさ×信頼度×今日の条件との一致度）

過去4走の基準比較指数を単純平均すると、芝ダ混在・距離バラバラな過去走がそのまま同列に扱われてしまうため、以下3つの重みを掛け合わせて加重平均しています（`src/utils/raceWeighting.ts`）。

| 軸 | 内容 |
|---|---|
| 新しさ | 前走1.0 → 2走前0.8 → 3走前0.6 → 4走前0.4 |
| 基準タイムの信頼度 | その走の基準タイムのサンプル数が50件以上あれば満額、それ未満は按分（`sampleCount / 50`） |
| 今日の条件との一致度 | サーフェスが違えば重み0.3に割引。距離差は800mでおよそ0.2まで減衰（下限0.2） |

#### 信頼度（偏差値の隣に表示）

上記の重みの合計が「理論上の最大値（全4走が新しさ満額×信頼度満額×条件完全一致だった場合）」に対してどのくらいかを0〜100%で表示します。低いほど「過去走の大半が今日と条件違い、または基準タイムのサンプルが薄い」ことを意味し、偏差値をどこまで信用してよいかの目安になります。

#### 上がり3F基準比較

その馬の過去走の上がり3Fを、同条件の**レース全体**の参考上がり3F基準と比べたzスコアです。個々の馬ごとの上がり3F基準ではなく、レース単位の基準と比較している点に注意してください（PDFの成績データにはレース全体の参考値しか載っていないため）。プラスが大きいほど基準よりキレる上がりだったことを示します。

#### 失速傾向 / 好走の上がり（バッジ）

上記の基準比較とは別の指標で、**その馬自身のレース内でのペース配分**を見ています。前半の平均ペースから逆算した「上がり3Fの期待タイム」と実際の上がり3Fの差分（`last3FExcessSeconds`）が、固定閾値（+0.5秒で失速、-0.5秒で好走）を超えたかどうかで判定します。過去4走のうち半数以上が失速判定なら「失速傾向」、好走判定なら「好走傾向」を馬単位のバッジとして表示します。

#### 参考値表示

基準タイムのサンプル数が20件未満の場合、「参考値 n=◯」と表示されます。統計的な裏付けが薄いことを示すだけで、値そのものが無効というわけではありません。

### 2-4. データの更新

`src/data/baseline.json`・`src/data/last3fBaseline.json`・`src/data/jockeyZScore.json` は、jra-batchの出力をビルド時に静的にバンドルしているだけです。実行時の通信は一切発生しません。最新のデータを反映したい場合は、1-6の手順でコピーし直してから `npm run build`（または `npm run dev` を再起動）してください。

---

## 3. 既知の制約・今後の拡張候補

- **オッズ・馬体重・性齢は分析に未使用**：`race_facts_<年>.jsonl`には既に含まれていますが（`odds`/`bodyWeight`/`bodyWeightChange`）、Webアプリの分析ロジックにはまだ組み込んでいません
- **枠番・馬番は未パース**：内枠/外枠バイアスの分析はまだできません
- **脚質（逃げ・先行・差し・追込）は未対応**：PDFにはコーナー通過順位の生データがあるものの、まだ利用していません
- **クラス昇級・降級の判定は未対応**：`raceClassText`/`grade`は生データにあるが、「今日のクラス」との比較機能はまだ実装していません
- **出走間隔（休み明け・連闘）の分析は未対応**：過去走の日付はすでに取得済みなので、追加のPDF解析なしで実装可能
- **フォームの上昇/下降トレンドは未対応**：直近走ほど基準比較指数が良化/悪化しているかの検出
